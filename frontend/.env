# Use the real ASP.NET Core backend instead of the in-browser mock.
# Requests to /api are proxied to http://localhost:5000 by vite.config.js.
# Set to true (or delete this file) to fall back to the mock for demos.
VITE_USE_MOCK=false
